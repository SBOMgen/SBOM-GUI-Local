import { PackageJson } from 'read-pkg-up';
interface PackageJsonInfo {
    packageJson?: PackageJson;
    packageJsonPath?: string;
}
export type PathExists = (path: string) => Promise<boolean>;
export interface Context extends PackageJsonInfo {
    pathExists: PathExists;
    nodeVersion: string;
}
export declare const getPackageJson: (projectDir: string) => Promise<PackageJsonInfo>;
export declare const getContext: (projectDir?: string, nodeVersion?: string) => Promise<Context>;
export {};
